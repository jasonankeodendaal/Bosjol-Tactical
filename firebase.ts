// This file is now empty, as Firebase is no longer used in the application.

// Placeholder exports to avoid breaking existing imports.
// In a real application migrating fully away from Firebase,
// you would replace all usages of these exports with your API calls.
export const USE_FIREBASE = false;
export const firebaseConfig = {};
export const isFirebaseConfigured = () => false;
export const firebaseInitializationError: Error | null = null;
export const auth = null;
export const db = null;
export const storage = null;
export const firebase = {};

